/** Normalized local progress within countdown transition where the white flash peaks. */
export const COUNTDOWN_FLASH_PEAK_AT = 0.12;

/** Normalized local progress where the white flash has fully cleared. */
export const COUNTDOWN_FLASH_END_AT = 0.2;

/** Normalized local progress where pure blackout holds before typography appears. */
export const COUNTDOWN_BLACKOUT_HOLD_END_AT = 0.3;

/** Normalized local progress where ceremony meta copy begins fading in. */
export const COUNTDOWN_META_FADE_START_AT = 0.3;

/** Normalized local progress where ceremony meta copy reaches full opacity. */
export const COUNTDOWN_META_FADE_END_AT = 0.5;

/** Normalized local progress where the live countdown begins fading in. */
export const COUNTDOWN_TIMER_FADE_START_AT = 0.3;

/** Normalized local progress where the live countdown reaches full opacity. */
export const COUNTDOWN_TIMER_FADE_END_AT = 0.55;
