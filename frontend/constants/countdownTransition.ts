/** Normalized local progress within the countdown transition where the white flash peaks. */
export const COUNTDOWN_FLASH_PEAK_AT = 0.32;

/** Normalized local progress where the white flash has fully cleared. */
export const COUNTDOWN_FLASH_END_AT = 0.52;

/** Normalized local progress where the parchment overlay begins fading in. */
export const COUNTDOWN_OVERLAY_FADE_START_AT = 0.52;

/** Normalized local progress where the parchment overlay reaches full opacity. */
export const COUNTDOWN_OVERLAY_FADE_END_AT = 0.88;
