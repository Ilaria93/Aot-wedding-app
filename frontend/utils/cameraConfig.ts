/** Default perspective camera settings for the hero canvas. */
export const HERO_CAMERA_FOV = 60;

export const HERO_CAMERA_POSITION: [number, number, number] = [0, 2.2, 6];

export const HERO_CAMERA_NEAR = 0.1;

/** Covers the full Operation Ravenna camera path (z ≈ -305) with headroom. */
export const HERO_CAMERA_FAR = 600;
