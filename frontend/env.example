# Copy to .env (never commit .env). Expo loads EXPO_PUBLIC_* at build time.

# FastAPI backend URL (use your machine IP on physical devices, not localhost).
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000

# Admin key for admin screens and local invite generation helper.
EXPO_PUBLIC_ADMIN_API_KEY=
